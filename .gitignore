.Rproj.user
.Rhistory
.RData
.Ruserdata
.DS_Store
.antigravitycli/
# Node / Vite frontend
node_modules/
dist/
*.tsbuildinfo
.env
.env.local
.env.*.local
*.local
jr_tng_reventlow.sql
# Afledt analyse af TNG-dumpet — kan indeholde levende-persondata (GDPR, invariant #8)
docs/tng-reventlow-analyse.md
# TNG-QA-afledte filer (crosswalk/review-kø/duckdb) — mapper til levende-data (GDPR, invariant #8)
data/tng.duckdb
data/tng-crosswalk.csv
data/tng-review-queue.csv
data/tng-corroboration.csv
# geo-enrich-afledte filer (samme begrundelse: afledt af TNG-dumpet)
data/geo-tng.duckdb
data/geo-crosswalk.csv
data/geo-geocode-candidates.csv
data/geo-geocode-cache.csv

# DAA-udtræk arbejdsmappe (kan indeholde levende-persondata)
work/
work_presens/

# Kilde-PDF'er (store, ophavsret) — ikke i repo
*.pdf

# Udtrukne billeder fra DAA (store, ophavsret) — på disk, ikke i repo
media/
web/vite.config.js
web/vite.config.d.ts

# Agent-worktrees (harness-sandkasser)
.claude/worktrees/

# Redundante design-bundles (udpakket indhold ligger i design/)
*.zip

# Supabase CLI-link-state (project-ref/pooler-URL — infra-id, aldrig commit)
supabase/.temp/
# MCP-config (indeholder Supabase project-ref — infra-id, lokal pr. maskine)
.mcp.json

# Brainstorming visual companion (throwaway mockups)
.superpowers/
